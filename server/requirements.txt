# echo-server dependencies.
#
# Everything here must install on Windows from a prebuilt wheel -- no local
# compilation, no MSVC toolchain. Verified against CPython 3.11 (x64).
#
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   pip install -r server/requirements.txt

# --- transport: WebSocket hub + HTTP display API in one library -------------
aiohttp>=3.9,<4

# --- Gemini Live API --------------------------------------------------------
google-genai>=1.0,<2

# --- wake word --------------------------------------------------------------
# openwakeword pulls numpy/scipy/tqdm. onnxruntime is the inference backend we
# pin explicitly so it is never resolved to the tflite path (no Windows wheel).
openwakeword>=0.6,<0.7
onnxruntime>=1.16,<2
numpy>=1.24,<3

# --- config -----------------------------------------------------------------
python-dotenv>=1.0,<2

# --- tests / harness --------------------------------------------------------
pytest>=8.0,<9
pytest-asyncio>=0.23,<2
httpx>=0.27,<1

# --- Govee BLE control ------------------------------------------------------
bleak>=3.0,<4
