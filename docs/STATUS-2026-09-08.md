# STATUS — 2026-09-08 (overnight run: brief 7 v2 verified, Spotify split, stopwatch, layout engine)

_Recorded by Hermes at 03:45 ET. Commits: `c5af7e1` (head), pushed to origin/main.

## What shipped overnight

### HARDWARE-BRIEF-7 v2 — button input model (verified by owner pre-bed)
- Mic button: short press = talk toggle (idle→chat, mid-chat→end, muted→unmute+talk).
- Hold ≥ 1 s = privacy mute (server log confirmed ON/OFF flips at 01:14).
- Screen taps are UI-only (tap-to-chat retired; server ignores idle taps).
- gating.kl: `key 116 F1` on the running ROM (MICMUTE label unsupported on A11;
  F1 = pass-through keycode). App listens for KEYCODE_F1 (131).
- Audio cue: double-beep on idle→listening (wake or button).

### Spotify — two-app credential split (kills the shared-id 429 walls)
- `token.json` (librespot app id) drives librespot's session; `spotify_creds_web/`
  (Jaiden's personal app `fa17fb…`) drives the Web API. Server: 496 tests green.
- librespot authenticated + spirc active on the current server.

### CLOCK-BRIEF — stopwatch (server + client, deployed to the device)
- Server: `alarm_command` ops stopwatch_{start,pause,reset,lap,status} + device→server
  `stopwatch_state`; coordinator round trips; 5 Gemini tools; tests.
- Client: `Stopwatch` (persisted, wakelock, laps), ScheduleScreen stopwatch page
  (big readout + Start/Pause/Lap/Reset + lap list), native tile + WebView chip,
  AlarmService hosts a running stopwatch.

### UI-BRIEF-14 — layout engine (SERVER SLICE ONLY; client home rebuild = NEXT)
- `layout {focus: home|music|chat}` message + hub arbitration (chat > music > home)
  driven by session state + the Spotify card hook. Tests green.
- **Client visual rebuild (65/35 home, chat/music templates, transitions) not
  done** — the WebView ignores `layout` messages for now (harmless).

## Device state
- Server running (tracked, PID via `netstat` on 8766), device connected (v1.6).
- New APK installed (stopwatch UI + cue + F1 button); server has the new code.

## Next up
1. **UI-BRIEF-14 client**: the WebView 65/35 home + focus templates + transitions
   (index.html / app.js / styles.css + the layout message consumer in MainActivity).
2. Owner verify round: Spotify playback ("play some lo-fi"), stopwatch by voice +
   screen, audio cue, mute persistence across reboot.
3. UI-BRIEF-15 (music focus) after 14's templates land.

## Notes
- `.env` now has SPOTIFY_API_CLIENT_ID (personal app); SPOTIFY_CLIENT_ID removed
  (defaults to the librespot id). Web creds dir gitignored.
- Camera: still at the documented HAL-userspace wall (owner's give-up call stands).
