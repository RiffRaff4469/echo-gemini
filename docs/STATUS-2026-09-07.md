# STATUS — 2026-09-07 (stopping point `de622f5`)

_Recorded by Hermes at the owner's request — pick a clean stopping point and
document it. This snapshot is the "where we left it" handoff. The roadmap lives
in [ROADMAP.md](../ROADMAP.md); this file is the day-by-day record._

## The stopping point

- **Repo state**: local `main` at `de622f5` — _spotify: close the test gaps left
  by brief 12 (config block, v1.5 client protocol)_. Working tree **clean**.
  2026-09-06 22:45 ET. 23 commits newer than what GitHub had — pushed same day.
- **In-flight lane (another agent)**: the ROM/camera workstream continues in the
  WSL build environment (device side). This snapshot does not freeze that lane;
  it records where the repo's code/docs stood.

## Device state (2026-09-06 ~21:47, most recent known)

- **Custom ROM live via dirty flash, no wipe**: `lineage-18.1-20260906-UNOFFICIAL-
  checkers.zip` installed over running v0.6 in TWRP. Boots straight to launcher
  in ~1 min — no wizard, /data preserved (provisioned flags, saved Wi-Fi,
  Tailscale login, installed APKs, permissions).
- Post-boot re-does (all done 2026-09-06): Wi-Fi re-joined via
  `cmd wifi connect-network AirOrangeX open` (saved network does NOT auto-join
  across ROM swap — OWE quirk), one manual Tailscale launch (tunnel came up,
  node **echo-show-5-1**), screensaver-off re-asserted, EchoTerminal v1.4
  connected to the server 21:47:13 from the tailnet.
- **Not yet verified on the custom ROM** (owner queue): touch axes, camera
  ("what do you see"), Bluetooth pairing. Expected fine — kernel config and
  `.idc` files are byte-identical to v0.6.
- **Rollback path (proven)**: dirty-flash v0.6 zip back over the custom ROM —
  no wipe needed going back either; ~15 min. Zip kept at
  `tools/echo-show-ref/stage/lineage-18.1-20260624-UNOFFICIAL-checkers.zip`.

## What shipped in the run to this stopping point

| Date | Thing | Evidence |
|---|---|---|
| 09-03 | Client compiles; toolchain on PC (JDK17/SDK34/Gradle 8.4) | `app-debug.apk` |
| 09-03→06 | Phase 1 flash: rooted LOS 18.1 v0.6 + verified backup | `docs/FLASHING.md` |
| 09-06 | v2 alarms/timers client (`ad4d2e8`), APK on Show | brief 3 |
| 09-06 | Tailscale on-device, always-on, survives reboots | node `echo-show-5-1` |
| 09-06 | Mic measured: +24–28 dB software gain mandated | `docs/HARDWARE-STATUS.md` §1.1 |
| 09-06 | Tap semantics v1.4 — tap ends chat (`d67b39e`) | fix brief 11 |
| 09-06 | Jarvis speech rules — one short sentence (`c3533b7`) | prompt file |
| 09-06 | Custom ROM FINAL BUILT (camera+BT+WebView), dirty-flashed | `lineage-18.1-20260906-…zip` |
| 09-06 | Spotify brief 12 code complete + test gaps closed (`de622f5`) | brief 12 + finish |

## Open items carried forward (see ROADMAP "Verify queue")

1. Touch / camera / BT verification on the custom ROM (owner, ~10 min).
2. Far-field mic take → set `MIC_GAIN` (+24–28 dB) and tune wake threshold.
3. Alarm audio on-device test.
4. Spotify end-to-end: librespot binary → one-time login → first audio on the Show.

## Notes for the next session / agent

- Always `git status` first: workers have left uncommitted files behind before
  (Hermes committed them as `d67b39e` after brief 11's worker exited).
- `.env` lives at repo root (`ECHO_PORT=8766`, keys inside). Never commit it.
- Server verify loop: `pytest server/ -q` → run with a free port → drive with
  `server/tools/fake_device.py`.
- Full operational detail: the `echo-gemini-appliance` operator skill
  (local Hermes install) — this repo's docs are the public map, the skill has
  the environment-specific runbooks.
